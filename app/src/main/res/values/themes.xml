<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Examen3_Kevin_Funes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Examen3_Kevin_Funes" parent="Base.Theme.Examen3_Kevin_Funes" />

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- Configura el color de fondo del diálogo -->
        <item name="android:windowBackground">@color/colorDialogBackground</item>
        <!-- Configura el color del texto -->
        <item name="android:textColorPrimary">@color/colorDialogText</item>
        <!-- Configura el color de los botones -->
        <item name="android:textColorSecondary">@color/colorDialogButton</item>
        <!-- Otros atributos que desees configurar -->
    </style>
</resources>